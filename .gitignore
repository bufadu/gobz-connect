# Build outputs — anchored to repo root so they don't also match the
# cmd/gobz-connect/ source directory.
/gobz-connect
dist/

# Local development config and secrets
config.yaml
qobuz-secrets.json
*.json

# Temp build directory used by install.sh
/tmp/gobz-connect-build/
